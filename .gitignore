# Rust specific
/target/
**/*.rs.bk

# If you have generated documentation
/target/doc/

# These are backup files generated by rustfmt
**/*.rs.bk

# If you use the `cargo install` command to install binaries
/cargo-install.log

# If you use the `cargo test` command to run tests
/cargo-test.log

# If you use the `cargo bench` command to run benchmarks
/cargo-bench.log

# If you use the `cargo build` command to build your project
/cargo-build.log

# If you use the `cargo run` command to run your project
/cargo-run.log

# If you use the `cargo check` command to check your project
/cargo-check.log

# If you use the `cargo doc` command to generate documentation
/cargo-doc.log

# If you use the `cargo clean` command to clean your project
/cargo-clean.log

# If you use the `cargo update` command to update dependencies
/cargo-update.log

# If you use the `cargo publish` command to publish your project
/cargo-publish.log

# If you use the `cargo login` command to log in to crates.io
/cargo-login.log

# If you use the `cargo logout` command to log out of crates.io
/cargo-logout.log

# If you use the `cargo owner` command to manage crate owners
/cargo-owner.log

# If you use the `cargo yank` command to yank a version of a crate
/cargo-yank.log

# If you use the `cargo unyank` command to unyank a version of a crate
/cargo-unyank.log

# If you use the `cargo vendor` command to vendor dependencies
/cargo-vendor.log

# If you use the `cargo verify-project` command to verify a project
/cargo-verify-project.log

# If you use the `cargo verify` command to verify a crate
/cargo-verify.log

# If you use the `cargo verify-deps` command to verify dependencies
/cargo-verify-deps.log

# If you use the `cargo verify-index` command to verify the index
/cargo-verify-index.log

# If you use the `cargo verify-lockfile` command to verify the lockfile
/cargo-verify-lockfile.log

# If you use the `cargo verify-manifest` command to verify the manifest
/cargo-verify-manifest.log

# If you use the `cargo verify-metadata` command to verify metadata
/cargo-verify-metadata.log

# If you use the `cargo verify-package` command to verify a package
/cargo-verify-package.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-signature` command to verify a signature
/cargo-verify-signature.log

# If you use the `cargo verify-source` command to verify the source
/cargo-verify-source.log

# If you use the `cargo verify-target` command to verify the target
/cargo-verify-target.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-toolchain` command to verify a toolchain
/cargo-verify-toolchain.log

# If you use the `cargo verify-workspace` command to verify a workspace
/cargo-verify-workspace.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo# Rust specific
/target/
**/*.rs.bk

# If you have generated documentation
/target/doc/

# These are backup files generated by rustfmt
**/*.rs.bk

# If you use the `cargo install` command to install binaries
/cargo-install.log

# If you use the `cargo test` command to run tests
/cargo-test.log

# If you use the `cargo bench` command to run benchmarks
/cargo-bench.log

# If you use the `cargo build` command to build your project
/cargo-build.log

# If you use the `cargo run` command to run your project
/cargo-run.log

# If you use the `cargo check` command to check your project
/cargo-check.log

# If you use the `cargo doc` command to generate documentation
/cargo-doc.log

# If you use the `cargo clean` command to clean your project
/cargo-clean.log

# If you use the `cargo update` command to update dependencies
/cargo-update.log

# If you use the `cargo publish` command to publish your project
/cargo-publish.log

# If you use the `cargo login` command to log in to crates.io
/cargo-login.log

# If you use the `cargo logout` command to log out of crates.io
/cargo-logout.log

# If you use the `cargo owner` command to manage crate owners
/cargo-owner.log

# If you use the `cargo yank` command to yank a version of a crate
/cargo-yank.log

# If you use the `cargo unyank` command to unyank a version of a crate
/cargo-unyank.log

# If you use the `cargo vendor` command to vendor dependencies
/cargo-vendor.log

# If you use the `cargo verify-project` command to verify a project
/cargo-verify-project.log

# If you use the `cargo verify` command to verify a crate
/cargo-verify.log

# If you use the `cargo verify-deps` command to verify dependencies
/cargo-verify-deps.log

# If you use the `cargo verify-index` command to verify the index
/cargo-verify-index.log

# If you use the `cargo verify-lockfile` command to verify the lockfile
/cargo-verify-lockfile.log

# If you use the `cargo verify-manifest` command to verify the manifest
/cargo-verify-manifest.log

# If you use the `cargo verify-metadata` command to verify metadata
/cargo-verify-metadata.log

# If you use the `cargo verify-package` command to verify a package
/cargo-verify-package.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-signature` command to verify a signature
/cargo-verify-signature.log

# If you use the `cargo verify-source` command to verify the source
/cargo-verify-source.log

# If you use the `cargo verify-target` command to verify the target
/cargo-verify-target.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-toolchain` command to verify a toolchain
/cargo-verify-toolchain.log

# If you use the `cargo verify-workspace` command to verify a workspace
/cargo-verify-workspace.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log

# If you use the `cargo verify-vendor` command to verify a vendor
/cargo-verify-vendor.log

# If you use the `cargo verify-version` command to verify a version
/cargo-verify-version.log

# If you use the `cargo verify-warn` command to verify a warning
/cargo-verify-warn.log

# If you use the `cargo verify-error` command to verify an error
/cargo-verify-error.log

# If you use the `cargo verify-fix` command to verify a fix
/cargo-verify-fix.log

# If you use the `cargo verify-lint` command to verify a lint
/cargo-verify-lint.log

# If you use the `cargo verify-test` command to verify a test
/cargo-verify-test.log

# If you use the `cargo verify-bench` command to verify a benchmark
/cargo-verify-bench.log

# If you use the `cargo verify-doc` command to verify documentation
/cargo-verify-doc.log

# If you use the `cargo verify-clean` command to verify a clean
/cargo-verify-clean.log

# If you use the `cargo verify-update` command to verify an update
/cargo-verify-update.log

# If you use the `cargo verify-publish` command to verify a publish
/cargo-verify-publish.log

# If you use the `cargo verify-login` command to verify a login
/cargo-verify-login.log

# If you use the `cargo verify-logout` command to verify a logout
/cargo-verify-logout.log

# If you use the `cargo verify-owner` command to verify an owner
/cargo-verify-owner.log

# If you use the `cargo verify-yank` command to verify a yank
/cargo-verify-yank.log

# If you use the `cargo verify-unyank` command to verify an unyank
/cargo-verify-unyank.log